package com.neelastack.entity;

/** Where a meeting happens. See booking-engine master prompt section 22. */
public enum LocationType {
    GOOGLE_MEET,
    ZOOM,
    TEAMS,
    PHONE,
    IN_PERSON,
    CUSTOM
}
